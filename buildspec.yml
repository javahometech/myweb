version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - echo Build started on `date`
      - mvn clean package -DskipTests=true
artifacts:
  files:
    - target/myweb*.war
  discard-paths: yes
